/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['127.0.0.1'], // Allow loading images from your Django backend
    },
  };
  
  module.exports = nextConfig;
  